import CheckProductScreen from '@/src/components/check/CheckProductScreen';

/** Legacy route retained for existing Shop and contextual deep links. */
export default function ShopScanRoute() {
  return <CheckProductScreen />;
}
