import CheckProductScreen from '@/src/components/check/CheckProductScreen';

/** Scanner-first root uses the same Check implementation as /shop/scan. */
export default function CheckRoute() {
  return <CheckProductScreen />;
}
