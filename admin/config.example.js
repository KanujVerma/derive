// Copy to config.js. Both values are public browser configuration.
// Never place a service-role key, model key, or Stripe secret here.
window.DERIVE_ADMIN_CONFIG = Object.freeze({
  supabaseUrl: "http://127.0.0.1:54321",
  supabasePublishableKey: "",
});
